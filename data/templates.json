[
    {
        "id": "static",
        "name": "Static site",
        "description": "Simple static site on /var/www/<domain>",
        "content": "server {\n    listen 80;\n    server_name {{domain}};\n\n    # SSL_AUTOMATION_MARKER\n\n    root /var/www/{{domain}};\n    index index.html;\n\n    location / {\n        try_files $uri $uri/ =404;\n    }\n}\n"
    },
    {
      "id": "reverse-proxy-node",
      "name": "Reverse proxy to Node app",
      "description": "Proxy to http://127.0.0.1:3000",
      "content": "server {\n    listen 80;\n    server_name {{domain}};\n\n    location / {\n        proxy_pass http://127.0.0.1:3000;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n    }\n}\n"
    }
  ]
  